<?xml version="1.0" encoding="utf-8"?>
<!-- Platform Material NoActionBar parents so we pull in NO
     appcompat/material Gradle dependency (Slint draws its own
     fullscreen UI). Day/night split exists only for the cold-
     start window flash: Light here, dark in values-night. -->
<resources>
    <style name="Theme.Meditate" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
