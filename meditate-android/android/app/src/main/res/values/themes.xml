<?xml version="1.0" encoding="utf-8"?>
<!-- Platform Material NoActionBar parent so we pull in NO
     appcompat/material Gradle dependency (xbuild shipped none;
     Slint draws its own fullscreen UI). Phase 8's theming pass
     can refine this now that we control res/. -->
<resources>
    <style name="Theme.Meditate" parent="@android:style/Theme.Material.NoActionBar" />
</resources>
