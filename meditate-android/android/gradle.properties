# Mirrors xbuild's gradle/gradle.properties. -Xmx2048m matters on
# the 15 GiB / ~1 GiB-swap dev laptop (pause Nextcloud during
# heavy builds — see ANDROID_PORT.md memory-ceiling note).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
